# PostgreSQL
POSTGRES_PASSWORD=changeme_postgres

# Redis
REDIS_PASSWORD=changeme_redis

# Temporal (uses same Postgres instance, separate databases)
TEMPORAL_POSTGRES_PASSWORD=changeme_temporal

# MinIO
MINIO_ROOT_PASSWORD=changeme_minio

# Encryption
FACTORY_MASTER_KEY=changeme_must_be_32_bytes_hex

# Node environment
NODE_ENV=development
